pipeline {
    agent any
   
    stages {

      stage('all')
            {
                steps
                 { 
                    sh 'mvn clean package test'
                 }
            }
        stage("build & SonarQube analysis") {
            agent any
            steps {
              withSonarQubeEnv('sonarqube') {
                sh 'mvn clean package sonar:sonar'
              }
            }
          }
        stage('nexus'){
        steps{
        nexusArtifactUploader artifacts: [[artifactId: 'phoenix', classifier: '', file: '/var/lib/jenkins/workspace/pipeline/target/phoenix-0.0.1-phoenix.jar', type: 'jar']], credentialsId: '5ad4e043-bd88-448b-92e6-6ef401e4f2cf', groupId: 'com.maven', nexusUrl: '18.224.155.110:8081/nexus', nexusVersion: 'nexus2', protocol: 'http', repository: 'devopstraining', version: '0.0.1-phoenix'
        }
        
        
        
        }
        
    }
}
